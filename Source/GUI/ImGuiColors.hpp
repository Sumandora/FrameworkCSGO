#ifndef GUI_IMGUICOLORS
#define GUI_IMGUICOLORS

#include "imgui.h"

namespace ImGuiColors {
	static ImColor white = ImColor(255, 255, 255, 255);
	static ImColor red = ImColor(255, 0, 0, 255);
	static ImColor black = ImColor(0, 0, 0, 255);
}

#endif